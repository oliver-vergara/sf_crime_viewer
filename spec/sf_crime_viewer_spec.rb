require 'spec_helper'

describe SfCrimeViewer do
  it 'has a version number' do
    expect(SfCrimeViewer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
